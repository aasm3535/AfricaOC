#ifndef NANO_H
#define NANO_H

/**
 * @brief Главная функция-точка входа для редактора nano.
 * * @param filename Имя файла для редактирования. Если файл не существует, он будет создан.
 */
void nano_main(const char* filename);

#endif // NANO_H
